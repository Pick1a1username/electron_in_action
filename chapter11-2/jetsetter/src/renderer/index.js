import React from 'react';
import ReactDOM from 'react-dom';
import './style.scss';

ReactDOM.render(
  <React.StrictMode>
    <h1>Hello World!</h1>
  </React.StrictMode>,
  document.getElementById('app')
);
