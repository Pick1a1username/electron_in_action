{
  "name": "firesale",
  "version": "1.0.0",
  "description": "",
  "productName": "Fire Sale",
  "main": "./app/main.js",
  "scripts": {
    "start": "electron .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-win": "electron-packager . --asar --platform=win32 --overwrite --out=build --icon=icons/Icon.ico",
    "build-linux": "electron-packager . --asar --platform=linux --overwrite --out=build --icon=icons/Icon.png"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "electron": "^8.5.2",
    "marked": "^1.0.0"
  },
  "devDependencies": {
    "electron-packager": "^14.2.1"
  }
}
